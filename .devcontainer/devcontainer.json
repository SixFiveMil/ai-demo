{
  "name": "AI Security Sandbox",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  
  // Step 1: Install Ollama natively on the sandbox host machine
  "postCreateCommand": "curl -fsSL https://ollama.com/install.sh | sh",
  
  // Step 2: Start the daemon, wait a brief moment for it to wake up, and build both bots
  "postStartCommand": "ollama serve > /dev/null 2>&1 & sleep 5 && ollama pull llama3 && ollama create vulnerable_bot -f vulnerable.txt && ollama create hardened_bot -f hardened.txt"
}